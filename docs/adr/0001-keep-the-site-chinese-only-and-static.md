# Keep the site Chinese-only and static

For at least the next 12 months, the public site supports Chinese content only and is delivered as a static export to GitHub Pages. Remove unused multilingual and generalized deployment abstractions instead of preserving them speculatively; reintroduce them only when a concrete second language or deployment target exists. This accepts some future rework in exchange for a smaller production surface and less harness drift now.
