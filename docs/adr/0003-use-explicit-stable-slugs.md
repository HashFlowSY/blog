# Use explicit stable slugs

Every post and project declares an explicit slug that is unique within its own content collection and uses a stable lowercase kebab-case URL format. A date may appear as a prefix but is not the entire naming rule, so multiple posts can be published on the same day. Filenames are only for local organization and never implicitly determine public URLs; published slugs should not change.
